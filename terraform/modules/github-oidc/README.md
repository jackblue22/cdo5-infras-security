# github-oidc

Placeholder để giữ layout tương thích với `temp/aiops/infra`.

Milestone 1 tập trung vào AWS infrastructure baseline. GitHub Actions OIDC deploy role nên bổ sung ở phase CI/CD deployment sau khi thống nhất repo owner, branch policy, environment promotion và remote Terraform state.
